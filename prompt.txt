API Projects v2 (activa única, start/stop automático) 

Contexto: Selección de proyecto existe; arrancar agente requiere otra llamada. 
 Objetivo: Al seleccionar proyecto activo, auto-arrancar el runner con su config; al cambiar de proyecto, stop del anterior. 
 Alcance y tareas: 

PATCH /projects/{id}/select → persiste, detiene runner si hay uno, inicia nuevo con config. 

Publicar type=project con status=active-changed. 
 Aceptación: Cambiar de proyecto conmuta el runner sin llamadas manuales a /agent/start. 