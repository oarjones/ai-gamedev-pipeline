Timeline Revert con “compensating actions” 

Contexto: revert es un stub. 
 Objetivo: Implementar revert para eventos que lo permitan (file ops, scene ops) con “acciones compensatorias”. 
 Alcance y tareas: 

Definir por tipo de paso una función undo(step) o compensate(step). 

Guardar metadatos suficientes en args_json/result_json para revertir (p. ej. ruta fichero, snapshot pre). 

UI recibe type=timeline con status=reverted|cannot. 
 Aceptación: Revert de un cambio de archivo restaura el contenido previo. 
 Notas: Registrar inmutables como “no revertible”. 