Adaptadores de Agente + selección de LLM/proveedor 

Contexto: El Gateway debe saber hablar con diferentes agentes/modelos. 
 Objetivo: Diseñar interfaz AgentAdapter y al menos un adapter real (p. ej. “CLI-generic” que wrappea a Gemini CLI). 
 Alcance y tareas: 

Crear services/adapters/base.py con send(prompt, context), on_stream(line). 

AI Gateway mapea proyecto→adapter según config (project.json). 

Implementar “CLI-generic”: formateo/escape de prompts, parseo de líneas (JSON:), tool-call detection. 
 Aceptación: Cambiar el adapter en config y ver comportamiento distinto sin tocar el resto del sistema. 
 Notas: Abierto a añadir “Gemini/Claude/OpenAI” después, pero esta tarea cubre el contrato. 